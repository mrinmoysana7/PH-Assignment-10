import Footer from "@/components/Footer";

const PaymentLayoutPage = ({ children }) => {
  return (
    <div>
      {children}
      <Footer></Footer>
    </div>
  );
};

export default PaymentLayoutPage;
